[color]
  ui = auto

[alias]
  ll = log --decorate --oneline --graph
  s = status
  ls-ignore = ls-files -v | grep -e '^[^H]'

[user]
  name = kato
  email = ippei.kato@v-crews.co.jp
[core]
  excludesfile = ~/.gitignore_global
  editor = /usr/bin/vim
